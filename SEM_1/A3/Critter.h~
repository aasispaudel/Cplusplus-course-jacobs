/*
CH08-320142
a3_p4.h
Aashish Paudel
aasispaudel@jacobs-university.de
*/

#include <iostream>
#include <string>

using namespace std;

class Critter {
 public:
  //Constructors
  Critter();
  Critter(string);
  Critter(string, int, double, double=15);
  Critter(string, int, double, double, double);
  //Parameters
  string name;
  int hunger;  
  double boredom;
  double height;
  double thirst;

 private:
  //To map the values between 0.0 and 1.0
  double func_1(int);
  //Convert labeled value to percentage
  int func_2(double);
};
