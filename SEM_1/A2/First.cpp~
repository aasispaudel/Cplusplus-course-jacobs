#include <iostream>
#include <First.h>

//using namespace First;

First::setId(int x) {
  First::id = x;
  
  return;
}

First::getId() {
  return id;
}

First::doubleId(int x) {
  id *= 2;
  
  return;
}
